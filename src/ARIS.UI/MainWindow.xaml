<Window x:Class="ARIS.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ARIS.UI"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="ARIS" Height="768" Width="1024" WindowState="Maximized"
        Background="#0a0a0a"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Loading overlay (shown during bootstrap) -->
        <Grid x:Name="LoadingOverlay" Grid.Row="0" Background="#0a0a0a" Visibility="Visible">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="ARIS" FontSize="48" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock x:Name="LoadingStatus" Text="Initializing..." FontSize="16" Foreground="#888" HorizontalAlignment="Center"/>
                <ProgressBar x:Name="LoadingProgress" IsIndeterminate="True" Width="200" Margin="0,20,0,0" Height="4"/>
            </StackPanel>
        </Grid>

        <!-- WebView2 (hidden during bootstrap) -->
        <wv2:WebView2 x:Name="webView" Grid.Row="0" Visibility="Hidden"/>

        <!-- Status Bar -->
        <StatusBar Grid.Row="1" Background="#1a1a1a">
            <StatusBarItem>
                <TextBlock x:Name="statusText" Text="Starting..." Foreground="#888"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
